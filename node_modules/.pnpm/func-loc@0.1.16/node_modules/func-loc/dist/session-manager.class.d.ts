import { ILocation } from './cache-amanger.class';
export interface ILocateOptions {
    sourceMap?: boolean;
}
export declare class SessionManager {
    private cache;
    private session;
    private post$;
    private scripts;
    clean(): Promise<boolean>;
    locate(fn: (...args: any) => any, opts?: ILocateOptions): Promise<ILocation>;
}
//# sourceMappingURL=session-manager.class.d.ts.map