export default function AdminDepartments() {
  return (
    <div>
      <h2 style={{ color: "var(--primary)" }}>Manage Departments</h2>
      <p style={{ color: "var(--text-muted)", marginTop: "0.5rem" }}>
        Super admin view for creating and managing all departments.
      </p>
    </div>
  );
}
